spring:
  application:
    name: data-engine-data-manager
    log:
      path: /root/Documents/output/dataengine
  config:
    import: classpath:base.yml,classpath:cache.yml,classpath:rpc.yml,classpath:datasource.yml,classpath:file.yml,classpath:sa-token.yml,classpath:prometheus.yml

dubbo:
  application:
    qos-port: 20001

server:
  port: 8083