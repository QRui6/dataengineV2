spring:
  application:
    name: data-engine-data-source
    log:
      path: /root/Documents/output/dataengine
  config:
    import: classpath:base.yml,classpath:datasource.yml,classpath:cache.yml,classpath:rpc.yml,classpath:sa-token.yml,classpath:prometheus.yml

dubbo:
  application:
    qos-port: 20002

server:
  port: 8083