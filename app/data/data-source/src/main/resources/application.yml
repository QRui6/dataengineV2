spring:
  application:
    name: data-engine-data-source
    log:
      path: /root/Documents/output/dataengine
  config:
    import: classpath:base.yml,classpath:datasource.yml,classpath:cache.yml,classpath:rpc.yml

dubbo:
  protocol:
    port: 33334
  application:
    qos-port: 22222

server:
  port: 8086