spring:
  application:
    name: data-engine-personal
  config:
    import: classpath:base.yml,classpath:cache.yml,classpath:datasource.yml,classpath:rpc.yml,classpath:sa-token.yml,classpath:prometheus.yml

dubbo:
  application:
    qos-port: 20004

server:
  port: 8086