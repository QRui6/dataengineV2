spring:
  application:
    name: data-engine-upload
    log:
      path: /root/Documents/output/dataengine
  config:
    import: classpath:base.yml,classpath:cache.yml,classpath:rpc.yml,classpath:file.yml
