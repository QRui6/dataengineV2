spring:
  cloud:
    nacos:
      discovery:
        server-addr: ${urban.carbon.nacos.server.url}
      config:
        server-addr: ${urban.carbon.nacos.server.url}
        file-extension: properties
        name: ${spring.application.name}